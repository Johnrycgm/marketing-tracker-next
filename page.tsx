"use client";

import MarketingTracker from './MarketingTracker';

/**
 * Entry point for the Marketing Schedule Tracker. Delegates rendering to the
 * MarketingTracker component.
 */
export default function Page() {
  return <MarketingTracker />;
}